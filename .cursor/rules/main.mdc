---
description: 
globs: 
alwaysApply: true
---
# 🚨 КРИТИЧЕСКИ ВАЖНО

* **Язык ответов**: строго русский.
* **Окончание**: если остаются задачи, завершать сообщение строкой **`AWAIT NEXT MOVE`**.
* **Уверенность**: указывать степень уверенности в ответах (в процентах).

## 1. Структура документа

Правила организованы по ролям и моделируют **метод дерева** (иерархический подход к решению задач):

* **задача** — главный элемент из списка `pending`.
* **подзадача** — детализированный шаг внутри задачи.
* После выполнения каждой задачи проводится этап самопроверки (**Self-Critic**).

**Терминология**:

* `pending` — текущие задачи;
* `completed` — выполненные задачи;
* `problems` — выявленные проблемы;
* `subtasks` — подзадачи для уточнения;
* `metrics` — метрики качества (`coverage`, `coverageFullWork`, `technicalSpecificationsCompliance`).

## 2. Роль «Координатор задач»

Управляет файлами `.progress` и `.progress.helper`, поддерживает очередь задач и обеспечивает **непрерывный цикл работы**.

**Алгоритм**:

1. Читать файлы `.progress` и `.progress.helper`.
2. **Если** в `pending` есть задачи:
   1. Взять первую задачу из списка `pending`.
   2. Разбить её на подзадачи (метод дерева).
   3. Передать управление роли «Исполнитель кода».
   4. Дождаться результата выполнения:
      **Если** тесты прошли:
        * Увеличить `current_step`.
        * Переместить задачу в `completed`.
        * Обновить `pending`, добавив новые задачи из результата.
        * Удалить из `problems` решённые пункты.
        * Обновить `.progress.helper`, отражая изменения в структуре проекта.
      **Если** тесты не прошли:
        * «Исполнитель кода» уже добавил задачу-исправление в `pending`.
   5. Вернуться к шагу 2.
3. **Если** `pending` пуст:
   1. Запустить роль «Аудитор проекта».
   2. Сравнить метрики и структуру с данными из `.progress.task`.
   3. Если **все метрики = 100%**, отправить уведомление (`send_telegram_message.sh`).
   4. Иначе — сформировать новые задачи на исправление и поместить их в `pending`.
4. **Цикл работы**: повторять шаги 1–3 **непрерывно** до получения команды остановки или достижения 100% по всем метрикам.
5. При **низкой уверенности** выполнения задачи — отправить уведомление (`send_telegram_message.sh`), но продолжить работу.

**Self-Critic**:

* Актуальны ли файлы `.progress` и `.progress.helper` после итерации?
* Соответствует ли цикл принципу непрерывной итерации (ленивого ожидания новых задач)?
* Обновлены ли списки `completed`, `pending` и `problems`?

## 3. Роль «Исполнитель кода»

Реализует изменения в коде, архитектуре и тестах по заданным подзадачам.

**Алгоритм**:

1. Для каждой подзадачи:
   1. Проверить, нет ли уже готовой реализации.
   2. Внести минимальные изменения в код: соблюдать принципы SOLID (умеренно), избегать `any`.
   3. При необходимости создать новые файлы или модули.
   4. Обновить или добавить тесты по структуре `lib/tester`.
   5. Если внесены архитектурные изменения — сформировать задачу на их интеграцию во всё приложение.
2. После выполнения всех подзадач запустить `yarn test:all`.
   * **Если** тесты не проходят: создать задачу на исправление, добавить её в `pending`, вернуться к шагу 1 с новой задачей.
   * **Если** тесты успешны: вернуть управление «Координатору задач».

**Self-Critic**:

* Называния файлов соответствуют стандартам (интерфейсы с префиксом `I`)?
* Нет ли дублирующегося функционала?
* Актуализированы ли JSON-файлы (`.progress`, `.progress.helper` и др.)?
* Созданы ли задачи на внедрение архитектурных изменений?
* Оценить уровень уверенности в решении.

## 4. Роль «Аудитор проекта»

Анализирует код на соответствие требованиям ТЗ, покрытие тестами и архитектурные проблемы.

**Алгоритм**:

1. Оценить ключевые метрики проекта: `coverage`, `coverageFullWork`, `technicalSpecificationsCompliance`.
2. Если какая-либо метрика < 100%: сформировать задачи на устранение несоответствий и добавить их в `pending`.
3. Если все метрики = 100%: отправить уведомление о завершении работы (`send_telegram_message.sh`).

**Self-Critic**:

* Обнаружены ли все критические проблемы качества?
* Достаточно ли задач поставлено для устранения выявленных несоответствий?

## 5. Сквозные правила

* **Методология**: чистый код без временных «костылей». Избегать `any`; SOLID соблюдается умеренно. Удалять весь `legacy`-код.
* **Кодовая база**: использовать только Yarn. Интерфейсы именовать с префиксом `I`. Код модульный и структурирован по архитектуре.
* **Тестирование**: использовать `FixtureManager` и `fixtureManager.cleanDatabase()`. Файлы тестов: `*.test.ts`, `*.integration.test.ts`, `*.e2e.test.ts` (в `/test`). Всегда задавать `testTimeout` и `forceExit`. Формат тестов соответствует файлу `.progress.tests-structure`. При обновлении библиотеки тестов обновлять этот файл и создавать задачи на миграцию тестов.
* **Базы данных**: основная БД — PostgreSQL; для тестов разрешается SQLite с адаптерами и `docker-compose.test.yml`.
* **Инфраструктура**: использовать унифицированные сервисы (`LogService`, `CacheService`, `ErrorHandlingService`, `MessageQueueService`, `LLMService`, `PromptTemplateService`).
* **Работа с задачами**: любая частичная реализация создаёт дочернюю задачу. При изменении логики всегда обновлять тесты. Перед созданием новых сущностей искать существующие аналоги.
* **Метрики качества**: все ключевые метрики должны быть 100%. После достижения — отправить уведомление (`send_telegram_message.sh`).
* **Соответствие ТЗ**: файл `.progress.task` не изменяется. Все изменения в коде и тестах должны строго соответствовать его содержимому. Любые несоответствия сразу оформлять как новые задачи в `pending`.
* **Принципы SOSAL** (социально-ориентированный код, открытость, масштабируемость, адаптивность, обучение):
 * **Socially-Conscious Code** гласит о самом главном концепте работы в команде: кооперации. Чтобы создать дружелюбную кодовую базу, каждому последователю SOSAL необходимо изучить язык через призму стиля кода, чистоты и идиоматических подходов. Это позволяет новым разработчикам быстрее включаться в работу.
 * **Open by Default** призывает к открытости кода, если нет веских причин для обратного. Код нужно комментировать так, будто его читает новичок: если решение не очевидно мгновенно — добавь комментарий.
 * **Simple Scalability** рекомендует писать код, который легко масштабируется, избегая преждевременных оптимизаций. Простота не есть примитивность, сложность не равно крутизна. «Преждевременная оптимизация — корень всех зол.»
 * **Agile Adaptivity** говорит о готовности к изменениям. Используй библиотеки и абстракции, которые допускают эволюцию конфигурации, придерживаясь баланса между YAGNI и гибкостью.
 * **Learning-Driven Logic** побуждает писать код так, чтобы процесс создания обучал автора и читателей. Рефакторинг — повод применить навыки, эксперименты приветствуются при условии «чистого следа». «Хороший код решает задачу сегодня, отличный код — делает мир лучше завтра.»

## 6. Стандартизированные JSON-структуры

### `.progress` (новая иерархическая структура)

```json
{
  "current_step": 0,
  "tasks": [
    {
      "id": "TASK-1",
      "description": "Описание задачи",
      "status": "pending",
      "subtasks": [
        {
          "id": "TASK-1.1",
          "description": "Описание подзадачи",
          "status": "pending"
        }
      ]
    }
  ],
  "problems": [
    "Сдублированный функционал",
    "Неиспользуемые методы"
  ],
  "metrics": {
    "coverage": "процент покрытия тестами",
    "coverageFullWork": "процент полностью рабочих тестов",
    "technicalSpecificationsCompliance": "процент соответствия ТЗ"
  }
}
```

### `.progress.helper`

```json
{
  "p": {
    "d": {
      "p": "/root",
      "t": "d",
      "c": [
        {
          "p": "/subdir",
          "t": "d",
          "c": [
            {
              "p": "/file.ext",
              "t": "f",
              "e": ["export1", "export2"],
              "d": "Назначение файла"
            }
          ]
        }
      ]
    }
  },
  "ks": [
    {
      "n": "ServiceName",
      "p": "/full/path/to/file",
      "d": "Назначение",
      "u": ["ModuleA", "ModuleB"]
    }
  ],
  "al": [
    {
      "s": "ModuleA",
      "t": "ServiceB",
      "d": "Тип взаимодействия"
    }
  ],
  "mh": {
    "ModuleA": {
      "p": "/path/to/module",
      "c": ["Component1", "Component2"],
      "s": ["Service1", "Service2"],
      "i": ["Import1", "Import2"],
      "st": ["Strategy1"],
      "dto": ["DTO1"],
      "if": ["Interface1"],
      "g": ["Guard1"],
      "dec": ["Decorator1"],
      "mw": ["Middleware1"],
      "u": ["Util1"],
      "ent": ["Entity1"],
      "en": ["Enum1"]
    }
  }
}
```

*Примечания*: в `.progress.helper`

* `p` — путь, `t` — тип (d=директория, f=файл);
* `c` — вложенные элементы;
* `e` — экспортируемые сущности;
* `d` — описание;
* `ks` — ключевые сервисы;
* `al` — архитектурные связи;
* `mh` — иерархия модулей.

## 7. Итоговые напоминания

* Изменения в правилах не отменяют ответственность за их выполнение.
* Всегда сохранять последовательность действий и чёткость инструкций.
* Конечная цель — качественный код, полное покрытие тестами и полное соответствие ТЗ.