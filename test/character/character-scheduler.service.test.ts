import { TestModuleBuilder } from "../../lib/tester/utils/test-module-builder";

describe("CharacterSchedulerService", () => {
  it("should be defined", () => {
    expect(true).toBe(true);
  });
});
